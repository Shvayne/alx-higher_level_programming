#!/usr/bin/python3
"""this module contains a function that checks if an object is inherited"""
def is_kind_of_class(obj, a_class):
    """checks for inheritance"""
    return (isinstance(obj, a_class))
